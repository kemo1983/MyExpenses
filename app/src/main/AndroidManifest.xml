<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ke.myexpenses" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/myexpense"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name="activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="activity.PopUpCalculator"
            android:label="Calculator"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog.MinWidth" />
        <activity
            android:name="activity.ExpensesFragment"
            android:theme="@style/MyMaterialTheme" />
        <activity android:name="activity.ExpenseDataList" ></activity>
        <activity android:name="activity.TransferFragment" ></activity>

    </application>

</manifest>
